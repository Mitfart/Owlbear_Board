# Shared scene boards use scene metadata

Shared scene boards are stored on Owlbear scene metadata rather than room metadata keyed by scene. They conceptually belong to a single scene, and storing them with the scene avoids orphaned room-level records when scenes change or are removed.
