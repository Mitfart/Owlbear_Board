import { describe, expect, it } from "vitest";
import { activeEditPresence, shouldBroadcastEditPresence, visibleEditPresence } from "./editingPresence";
const board = (visibility: "private" | "shared") => ({ id: "b", name: "b", scope: "scene" as const, visibility, allowedUserIds: ["allowed"], revision: 0, cellSizePx: 72, cellGapPx: 2, items: [], updatedAt: "now" });
describe("editing presence", () => { it("expires, respects private board access, and only broadcasts activity safe for the whole room", () => { const p = { playerId: "p", playerName: "Pat", boardId: "b", itemId: "i", expiresAt: 110 }; const legacyPrivate = { ...board("private"), ownerId: "owner", allowedUserIds: undefined }; expect(activeEditPresence(p, 100)).toEqual(p); expect(activeEditPresence(p, 110)).toBeUndefined(); expect(visibleEditPresence(p, board("private"), "PLAYER", "other", 100)).toBe(false); expect(visibleEditPresence(p, board("private"), "PLAYER", "allowed", 100)).toBe(true); expect(visibleEditPresence(p, legacyPrivate, "PLAYER", "owner", 100)).toBe(true); expect(shouldBroadcastEditPresence(board("private"))).toBe(false); expect(shouldBroadcastEditPresence(board("shared"))).toBe(true); }); });
